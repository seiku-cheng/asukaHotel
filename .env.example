# Database Configuration
# For development with SQLite (default)
DATABASE_URL="file:./dev.db"
# For PostgreSQL (production)
# DATABASE_URL="postgresql://username:password@localhost:5432/hotel_booking"

# JWT Secret Key
# Generate a strong secret key for JWT token signing
JWT_SECRET="your-secret-key-here"

# Google Maps API Configuration
# To enable Google Maps functionality:
# 1. Go to Google Cloud Console (console.cloud.google.com)
# 2. Create a new project or select existing project
# 3. Enable Maps JavaScript API
# 4. Create API credentials (API Key)
# 5. Restrict the API key to your domain for security
# 6. Replace the value below with your actual API key
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY="your-google-maps-api-key-here"

# Gmail SMTP Configuration
# To enable email functionality for booking confirmations:
# 1. Create a Gmail account or use an existing one
# 2. Enable 2-factor authentication on your Gmail account
# 3. Go to Gmail settings > Security > App Passwords
# 4. Generate an App Password for "Mail"
# 5. Use your Gmail address and the App Password below
GMAIL_USER="your-gmail@gmail.com"
GMAIL_APP_PASSWORD="your-16-digit-app-password"

# Development/Production Environment
NODE_ENV="development"